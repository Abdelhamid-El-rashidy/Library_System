# Admin Workspace

Place admin-facing pages and tools here.

Suggested structure:
- `frontend/` — admin dashboard, add/edit/delete book pages
- `backend/` — admin-only API helpers or testing scripts

Be sure to protect admin endpoints with `is_admin` checks.
